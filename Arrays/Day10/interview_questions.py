"""

Which sorting algorithm performs best on an almost sorted array?

Insertion Sort performs best on an almost sorted array. This is because it has a time complexity of O(n) in the best case scenario, which occurs when the array is already sorted or nearly sorted. Insertion Sort works by building a sorted portion of the array one element at a time, making it efficient for small or nearly sorted datasets.

"""